package yaroslavgorbach.reaction.data.exercise.geoSwitching.model

import yaroslavgorbach.reaction.R

enum class TaskVariant(val textRes: Int) {
    IS_SQUARE(R.string.is_square), IS_BLUE(R.string.is_blue)
}